
````
    connect 
        -h
        -host
        -p
        -port
        -n
        -name
        
    rule 
        -c -code code
        -r -rule "int16|int8|string|int19|..."
        -f  file    // unsupport
            
    send  -d "s str|int8 10|s string" name
        
        
````